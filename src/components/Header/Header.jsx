import React from 'react';

import './Header.css';

const Header = (props) => {
    return (
        <div className="header">
            Product Aggregator
        </div>
    );
}

export default Header;
